extends layout

block content

  header
    h2 Administer Delete
  
  section
    acticle
      form(method="post")
        label(for="ID") 관리자 삭제 (아이디)
        input(name="ID" type="text" placeholder="검색하세요.")
      button(type="submit" name="button") 삭제
